<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/main.css"/>
</head>

<body>
    <section class="hero min-h-screen" style="background-image: url('https://images.unsplash.com/photo-1704104501136-8f35402af395?q=80&w=1949&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D'); background-size: cover; background-position: top;">
        <div class="hero-overlay bg-opacity-60"></div>
        <div class="hero-content text-neutral-content text-center">
            <div class="max-w-md mx-auto">
                <h1 class="text-6xl font-extrabold">Memories of Saudi Arabia</h1>
                <p class="py-8 text-2xl">Explore the most popular places in the Kingdom of Saudi Arabia, all the way from Makkah to Medina and beyond.</p>
            </div>
        </div>
    </section>

    <div class="p-6 space-y-6">

        <div class="flex justify-end">
            <a href="/memories/add" class="btn btn-primary mb-4">Add Location</a>
        </div>

        <!-- I preferred doing the card/flex method over the table method, it looks way better and fits better with the sample code that was provided-->
        <% ProjectMemories.forEach(project => { %>
            <div class="card card-side bg-base-100 shadow-md flex flex-col md:flex-row">

                <figure class="md:w-1/3 w-full max-h-64 overflow-hidden">
                    <img src="<%= project.image %>" alt="Project Image" class="object-cover w-full h-full">
                </figure>
               
                <div class="card-body md:w-2/3 w-full">
                    <h2 class="card-title text-xl font-bold"><%= project.name %></h2>
                    <div>
                        <span class="badge badge-outline mb-2"><%= project.category %></span>
                    </div>
                    <p><strong>Address:</strong> <%= project.address %></p>
                    <p><strong>Comments:</strong> <%= project.comments %></p>
                    <div class="flex justify-end">
                        <a href="/memories/delete/<%= project.id %>" class="btn btn-error btn-sm" onclick="return confirm('Are you sure you want to delete this memory?')">Delete</a>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</body>

</html>